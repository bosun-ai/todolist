export interface Item {
  /**
   * Represents a single task in the to-do list application.
   * 
   * @property {string} task - The description of the task.
   * @property {number} priority - The priority level of the task, with lower numbers indicating higher priority.
   */
  task: string;
  priority: number;
}
